# Release v0.1.0

## Fix
- 修复 `src/pocketmine/PocketMine.php` 中版本号由 `1.1dev` 更新为 `0.1.0`。
- 修复 `src/pocketmine/Server.php` 中 `InCore Pro` 启动画面排版问题，统一缩进格式。
- 修复 `src/pocketmine/command/defaults/VersionCommand.php` 中版本信息输出逻辑：
  - 将核心开发代号信息改为通过 `getCodename()` 获取。
  - 将版本输出改为使用实际服务器版本 `getVersion()`，替代硬编码 `Genisys版本：1.1dev`。

## Changes
- `src/pocketmine/PocketMine.php` 版本常量 `VERSION` 设为 `0.1.0`。
- `src/pocketmine/command/defaults/VersionCommand.php` 版本命令显示内容更加准确。

## New
- 暂无新增功能。
